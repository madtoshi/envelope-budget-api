version: '3'
services:
  app:
    build: .
    ports:
      - 8080:8080
    env_file:
      - .env
    depends_on:
      - db
      - redis

  db: 
    image: postgres
    env_file:
      - .env
    volumes:
      - db-data:/db-data
  
  redis: 
    image: redis
    env_file:
      - .env
    volumes: 
      - cache-data:/cache-data

volumes:
  db-data:
  cache-data:
