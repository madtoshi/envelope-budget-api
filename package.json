{
  "name": "envelope-budget-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npx nodemon server.js",
    "new-model": "npx sequelize-cli model:generate --name",
    "migrate": "npx sequelize-cli db:migrate",
    "new-migration-file": "npx sequelize-cli migration:generate --name",
    "new-seed": "npx sequelize-cli seed:generate --name",
    "seed": "npx sequelize-cli db:seed:all",
    "undo-recent-seed": "npx sequelize-cli db:seed:undo",
    "undo-seeds": "npx sequelize-cli db:seed:undo:all",
    "undo-migrate": "npx sequelize-cli db:migrate:undo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "connect-redis": "^7.0.1",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-flash": "^0.0.2",
    "express-session": "^1.17.3",
    "express-validator": "^7.0.1",
    "method-override": "^3.0.0",
    "morgan": "^1.10.0",
    "node-cron": "^3.0.2",
    "nodemon": "^2.0.22",
    "passport": "^0.6.0",
    "passport-local": "^1.0.0",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "redis": "^4.6.6",
    "sequelize": "^6.30.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}
